node_modules/
dist/
dist-electron/
release/
.env
*.log
.DS_Store
Thumbs.db

# Android 构建产物（本地生成，不入库）
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/capacitor-cordova-android-plugins/build/
android/**/build/
*.apk

# 本地凭据
.capgo

# Claude Code local overrides
.claude/settings.local.json

# Dev / backup assets
resources/icon-original.png
resources/icon-preview.png
resources/resources/
scripts/make-icon.py

# 验收脚本的打包产物（由 scripts/verify/build.mjs 生成，仓库只保留 *.ts / *.html / serve.cjs / build.mjs）
scripts/verify/*.bundle.js

# 5.0 世界层的本地中间产物 / 研究草稿（不入库）
.gradle-build/
.gradle-user/
.tmp-preview/
.research/

# 生产环境变量（当前只有注释；将来可能放网关地址/令牌，绝不入库）
.env.production
